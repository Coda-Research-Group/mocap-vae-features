defaults:
  - _self_
  - exp: ???

beta: ???
latent_dim: ???
body_model: ???

resume: false

hydra:
  run:
    dir: ./runs/${hydra.runtime.choices.exp}/${hydra.job.override_dirname}
  sweep:
    dir: ./runs
    subdir: ${hydra.runtime.choices.exp}/${hydra.job.override_dirname}
  job:
    chdir: true
    config:
      override_dirname:
        exclude_keys:
          - resume
          - exp
